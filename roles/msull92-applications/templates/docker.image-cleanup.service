[Unit]
Description=Docker Image Cleanup Service
After=docker.service
Requires=docker.service

[Service]
Type=oneshot
TimeoutStartSec=0
ExecStart=/usr/bin/docker rmi $(/usr/bin/docker images --filter "dangling=true" -q --no-trunc)

[Install]
WantedBy=multi-user.target
